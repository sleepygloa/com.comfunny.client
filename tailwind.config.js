/** @type {import('tailwindcss').Config} */
    module.exports = {
      content: [
        "./src/**/*.{js,jsx,ts,tsx}", // src 폴더 안의 모든 js, jsx, ts, tsx 파일을 감시
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    }